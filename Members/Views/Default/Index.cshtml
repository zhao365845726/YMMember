@{
    ViewBag.Title = "个人中心";
    Layout = "~/Views/Shared/_LayoutMember.cshtml";
}

@Scripts.Render("~/bundles/jquery")

<style>
    #main{margin-top:25px;}
    /*#btnQuickOperation{display:block;}*/
</style>

<script>
    $(function () {
        $("#btnQuickOperation").removeClass("hidden");
        $("#bs-example-navbar-collapse-1").removeClass("hidden");
    });
</script>

@*<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <p class="navbar-text text-center">
            <b>个人中心</b>
            
            
        </p>
    </div>
</nav>*@

@*<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a id="btnBack" href="@Url.Action("Login",new { ID = TempData["ID"] })" class="glyphicon glyphicon-chevron-left" style="float:left;"></a>
            <p id="navbar-title" class="text-center">@ViewBag.Title</p>
            <a id="btnResetPwd" href="@Url.Action("ResetPwd",new { ID = TempData["ID"] })" class="glyphicon glyphicon-cog" style="float:right;"></a>
        </div>
    </div><!-- /.container-fluid -->
</nav>*@

<div id="main" class="container">
    <div class="panel panel-primary">
        <div class="panel-body">
            姓名：<span id="name">@TempData["Name"]</span>
        </div>
        <div class="panel-body">
            性别：<span id="sex">@TempData["Sex"]</span>
        </div>
        <div class="panel-body">
            年龄：<span id="age">@TempData["Age"]</span>
        </div>
        <div class="panel-body">
            病种：<span id="disease">@TempData["Disease"]</span>
        </div>
        <div class="panel-body">
            亚健康状态：<span id="subhealth">@TempData["Subhealth"]</span>
        </div>
    </div>
    <a class="btn btn-default btn-lg btn-block" href="@Url.Action("Edit",new { ID = TempData["ID"] })" style="margin-bottom:10px;">完善信息</a>
    <div class="panel panel-success">
        <div class="panel-heading">职务信息：</div>
        <div class="panel-body">
            <span id="groupnumber">@TempData["GroupNumber"]</span>
        </div>
    </div>

    <a class="btn btn-success" href="@Url.Action("InspectionList",new { ID = TempData["ID"] })">诊断记录</a>
    <a class="btn btn-info" href="@Url.Action("ConditionList",new { ID = TempData["ID"] })">调理记录</a>
</div>